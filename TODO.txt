troops!

set everything to be readonly in building and troop type files

maybe add the `default` keyword to important files like game store, game loop etc etc

use canvas for UI, more efficient than SVG, but with pixiJS
but maybe use SVG for menu or tech tree etc

add move interface and move queue,
so we can add handle collisions etc

POSSIBLY make it 2.5D? 

menu screen

pause screen (automatically toggled when leaving/rejoining the tab)

possibly get something in rendering so only visible hexes are rendered?
plus need to make sure if one hex updates, the entire grid shouldn't rerender,
only that hex, and even then only if its on the visible screen.
logic problem: the UI of a hex would rarely change - base info of a hex would change, yes, but what it looks like wouldn't really change?
building/troop icons? are they part of a hex's UI?
hex has building info on it, so if that changes i guess it would have to rerender, but hopefully only that hex?

write stuff into
GAME_DOCUMENTATION.txt
TODO.txt
README.md - this one is what people will first see,
so explain what this is and point to the website as well as the game docs which explain how the game works,
and also point to howToRun.txt if they wanna deploy themselves



Terrain; need to add different terrains to generateTerrain in systems/grid
at the moment its just grassland with mountains at the edges

also need to add terrain colour - enum probably



UI:
actually make a start;
sort out HTML
get the grid down
zooming/panning with bounds/limits
work out the perfect size and quantity of hexes
minimap
toolbar